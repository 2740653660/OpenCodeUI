# OpenCodeUI 个人工作区规则

> 本文件只存在于我的 fork（2740653660/OpenCodeUI）中，用于规定 AI 助手在本仓库的工作方式。
> 上游仓库（lehhair/OpenCodeUI）不受本文件影响，本文件也绝不允许进入发往上游的 PR。

## 使用者情况（最重要）

本仓库的使用者是**编程小白**：不懂项目开发、不懂 git、不懂技术术语。

AI 助手在本仓库工作时必须遵守：

1. 所有交流使用中文，用大白话解释，不假设使用者懂任何术语。
2. 出现技术概念（分支、PR、merge、冲突、回滚等）时，用简单易懂的比喻解释清楚。
3. 危险操作（删除、强制推送、回滚、解决合并冲突、修改 git 配置等）执行前，必须先说明后果并征得使用者同意。
4. 每完成一步，用一句话说明"做了什么、为什么这么做"。
5. 每次改动结束时，说明：改了什么、怎么验证的、有什么残余风险。

## 需求管理

- 使用者的个人需求统一存放在 `idea/` 文件夹，规则见 `idea/AGENTS.md`，必须严格遵守。
- `idea/` 文件夹随本 fork 的 git 管理（换机器可同步、有历史记录），但**绝不允许出现在发往上游的 PR 中**。

## 发往上游 PR 的红线

1. 发 PR 的分支必须基于上游最新代码创建：`git fetch upstream` 后从 `upstream/main` 切出分支。
   - 这样 PR 分支上天然没有 `idea/` 文件夹、根目录 AGENTS.md 等个人文件。
2. 发 PR 前必须检查 `git diff`，确认改动只包含本次需求相关的代码。
3. 如果对 PR 内容有任何不确定，先向使用者解释清楚，再决定是否发出。
4. 已解决的需求代码要合并回本地 `main`（使用者长期使用的版本），即使上游不接受 PR，功能也保留在自己 fork 中。

## 提交规范

- 提交信息使用中文。
- 除一行配置、错字等 trivial 小改外，提交信息包含标题和正文；正文说明：具体改动、验证结果、残余风险。
- 解决需求时按 `idea/AGENTS.md` 中的"需求解决 SOP"执行。

## 与上游同步

- 定期从 upstream 拉取上游新代码合并到本地 main（`git fetch upstream && git merge upstream/main`），保持 fork 不过时。
- 出现合并冲突时：先向使用者解释冲突是什么、怎么处理，征得同意后再解决。
